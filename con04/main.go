package main

func getMaxReqs(in string) string {
	ret := ""

	return ret
}

func main() {

	maxReqLen := 0
	// count := 11
	prevClient := 0
	prevPrevClient := 0
	reqs := []int{7, 1, 4, 1, 9, 1, 1, 9, 1, 7, 9}

	curReqLen := 0
	for k, v := range reqs {
		// if prevClient == 0 {
		// 	prevClient = v
		// 	curReqLen++
		// 	continue
		// }
		// if prevClient
		if k == 0 {
			prev
		}

	}

	// ret := getMaxReqs("100100100100100100100")
	// fmt.Println(ret)

	// return

	// in := bufio.NewReader(os.Stdin)
	// total := 0
	// fmt.Fscan(in, &total)

	// s := ""
	// for i := 0; i < total; i++ {
	// 	inp := ""
	// 	fmt.Fscan(in, &inp)
	// 	s += fmt.Sprintln(decode(inp))
	// }
	// fmt.Println(s)
}
